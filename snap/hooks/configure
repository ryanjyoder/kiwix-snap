#!/usr/bin/env bash

library=$(snapctl get library)

if [ -z "$library" ]; then
    exit 0
fi

if [ ! -r "$library" ]; then
    echo "'$library' is not readable"
    exit -1
fi

echo configs set
exit 0